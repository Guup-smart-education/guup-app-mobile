import styled from 'styled-components/native';

export const DataEmpty = styled.View`
  padding-bottom: ${({theme}) => theme.spacing.padding[25]};
  padding-top: ${({theme}) => theme.spacing.padding[25]};
  width: 100%;
`;
