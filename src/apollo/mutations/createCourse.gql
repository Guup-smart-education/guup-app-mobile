mutation createCourse($course: ICourse) {
  createCourse(course: $course) {
    ... on CreateCourse {
      createCourse
      success {
        message
      }
    }
    ... on ErrorResponse {
      error {
        message
      }
    }
  }
}
