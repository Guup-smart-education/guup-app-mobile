mutation createCourse($course: ICourse!, $metadata: IMetaData!) {
  createCourse(course: $course, metadata: $metadata) {
    ... on CreateCourse {
      createCourse
      success {
        message
      }
    }
    ... on ErrorResponse {
      error {
        message
      }
    }
  }
}
