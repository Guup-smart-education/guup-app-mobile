mutation createPost(
  $title: String
  $description: String!
  $photoURL: String
  $linkURL: String
  $ownerProfile: IProfile!
  $metadata: IMetaData
) {
  createPost(
    post: {
      title: $title
      description: $description
      photoURL: $photoURL
      linkURL: $linkURL
    }
    ownerProfile: $ownerProfile
    metadata: $metadata
  ) {
    ... on CreatePost {
      createPost {
        id
        owner
        title
        description
        photoURL
        linkURL
        viewsCount
        commentsCount
        clapsCount
        createdAt
        ownerProfile {
          displayName
          photoURL
          profission
        }
      }
      success {
        message
      }
    }
    ... on ErrorResponse {
      error {
        type
        message
      }
    }
  }
}
