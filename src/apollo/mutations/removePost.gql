mutation removePost($post: String!) {
  removePost(post: $post) {
    ... on RemovePost {
      post
      success {
        type
        message
      }
    }
    ... on ErrorResponse {
      error {
        type
        message
      }
    }
  }
}
